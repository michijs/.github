name: "Run command"
inputs:
  command:
    description: "Command to run"
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - run: bun run ${{ github.event.inputs.command }}
      shell: bash

